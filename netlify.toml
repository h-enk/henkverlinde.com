[build]
  command = "yarn build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "16.15.0"
  NPM_VERSION = "8.5.5"


[[plugins]]
  package = "netlify-plugin-submit-sitemap"

	[plugins.inputs]
    baseUrl = "https://henkverlinde.com"
    sitemapPath = "/sitemap.xml"
    ignorePeriod = 0
    providers = [
      "google",
      "yandex"
    ]
