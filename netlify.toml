[build]
  command = "npm run build"
  publish = "build"

[functions]
  directory = "functions"

[[headers]]
  for = "/*"
  [headers.values]
    Referrer-Policy = "strict-origin"
    X-Content-Type-Options = "nosniff"
