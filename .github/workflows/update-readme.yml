name: Readme

on:
  schedule:
    - cron: 0 8 * * *
  push:
    branches:
      - master

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://henkverlinde.com/blog/index.xml
          readme-section: feed