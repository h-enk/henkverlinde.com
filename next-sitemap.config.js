/** @type {import('next-sitemap').IConfig} */

const config = {
  siteUrl: 'https://henkverlinde.com',
  changefreq: 'weekly',
  priority: 0.7,
  sitemapSize: 5000,
  generateRobotsTxt: true
}

// export default config
module.exports = config
